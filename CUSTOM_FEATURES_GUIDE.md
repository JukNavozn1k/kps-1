# Руководство по кастомным признакам

## Обзор

Модуль **Feature Engineering** позволяет создавать новые признаки на основе математических операций над существующими признаками датасета. Это полезно для:

- 🔍 Поиска нелинейных зависимостей
- 📐 Создания взаимодействий между признаками
- 🎯 Улучшения качества прогноза моделиполний

## Поддерживаемые операции

### Бинарные операции (над двумя признаками)

| Операция | Формула | Пример | Применение |
|----------|---------|--------|-----------|
| **Произведение** | `x1 * x2` | коэффициент1 × коэффициент2 | Перемножение независимых факторов |
| **Отношение** | `x1 / x2` | odds_1 / odds_2 | Нормализация через делениеалт |
| **Сумма** | `x1 + x2` | odds_1 + odds_X | Комбинирование похожих величин |
| **Разность** | `x1 - x2` | odds_1 - odds_2 | Различие между факторами |

### Унарные операции (над одним признаком)

| Операция | Формула | Применение |
|----------|---------|-----------|
| **Квадрат** | `x²` | Нелинейные зависимости, усиление эффекта |
| **Куб** | `x³` | Сильно нелинейные зависимости |
| **Синус** | `sin(x)` | Периодические паттерны |
| **Косинус** | `cos(x)` | Периодические паттерны (сдвиг 90°) |
| **Тангенс** | `tan(x)` | Крутые нелинейные переходы |
| **Экспонента** | `exp(x)` | Экспоненциальный рост (с ограничением) |
| **Логарифм** | `log(\|x\|+1)` | Компрессия больших значений |
| **Корень квадратный** | `sqrt(\|x\|)` | Мягкая нелинейность |
| **Модуль** | `\|x\|` | Учёт абсолютного значения |

## Как использовать интерфейс

### Шаг 1: Открыть créateur признаков
В левой боковой панели найди раздел **"🔧 Кастомные признаки"** и кликни **"Создать новый признак"**

### Шаг 2: Выбрать операцию
- Из выпадающего списка выбери нужную операцию
- Автоматически покажется описание операции
- Укажи имя для нового признака

### Шаг 3: Выбрать исходные признаки
- Для унарных операций: выбери один признак (Признак 1)
- Для бинарных операций: выбери два признака (Признак 1 и Признак 2)

### Шаг 4: Добавить признак
Нажми кнопку **"✅ Добавить признак"**

Новый признак появится в списке **"Активные"** и будет автоматически добавлен к датасету перед обучением модели.

## Примеры использованияарь

### Пример 1: Произведение коэффициентов
```
Имя: odds_product_1_X
Операция: Произведение
Признак 1: odds_1
Признак 2: odds_X
```
Результат: Новый признак = odds_1 × odds_X

Зачем: Может отражать вероятность исхода матча

### Пример 2: Квадрат коэффициента
```
Имя: odds_1_squared
Операция: Квадрат
Признак 1: odds_1
```
Результат: Новый признак = odds_1²

Зачем: Усилит влияние больших коэффициентов

### Пример 3: Логарифм отношения
```
Имя: odds_ratio_log
Операция: Логарифм
Признак 1: (предварительно создаёшь через отношение)
```
Результат: log(|odds_1 / odds_2| + 1)

Зачем: Нормализирует широкий диапазон значений

## Взаимодействие с другими функциями

### Кастомные признаки + Фильтрация признаков
1. Создай кастомный признак (добавляется к датасету)
2. Затем в секции "Признаки" можешь выбрать какие признаки использовать для обучения
3. Кастомные признаки будут включены автоматически

### Кастомные признаки + Стандартизация
- Числовые кастомные признаки автоматически стандартизируются вместе с остальными, если включена опция "Стандартизация X"

### Кастомные признаки + Предикт
- При делании предикта созданные кастомные признаки применяются автоматически
- Нет необходимости входить значения для кастомных признаков вручную

## Советы и лучшие практики

### ✅ Делай так:
- **Начни с простых операций** - сначала пробуй квадраты, потом произведения
- **Анализируй результаты** - смотри метрики качества перед/после добавления новых признаков
- **Создавай осмысленные комбинации** - используй знание о предметной области
- **Постепенно добавляй признаки** - не добавляй слишком много сразу, может быть переобучение

### ❌ Не делай так:
- **Не создавай слишком много признаков** - это может привести к переобучению
- **Не используй сильно коррелированные признаки** - они несут дублирующую информацию
- **Не забывай про сложность модели** - больше признаков = больше параметров для обучения

## Технические детали

### Безопасность от ошибок
- Деление на ноль: защищено, используется очень малое значение вместо нуля
- Exp(x) overflow: ограничено значениями [-100, 100]
- log(x): используется log(|x| + 1) для работы с любыми значениями
- NaN и Inf: автоматически заменяются на нули

### Применение признаков
- Кастомные признаки добавляются к матрице X как дополнительные столбцы
- Порядок применения: исходные признаки + кастомные признаки
- При необходимости фильтрации выбираются из объединённого набора

## Удаление признаков

Нажми кнопку ❌ рядом с названием признака в списке "Активные", и он будет удалён из датасета перед обучением.

## Автоматические предложения

Нажми кнопку **"💡 Предложить интересную комбинацию"**, и система автоматически создаст несколько полезных комбинаций признаков на основе твоих данных.

## Нужна помощь?

Смотри примеры выше и экспериментируй! Каждый датасет требует своего подхода к feature engineering.
